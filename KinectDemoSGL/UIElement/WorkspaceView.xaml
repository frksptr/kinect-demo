<UserControl x:Class="KinectDemo.WorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             >
    <StackPanel Name="WorkspaceData" Orientation="Horizontal">

        <TextBlock Name="wsName" Width="Auto" MinWidth="60" Padding="2" Text="{Binding Path=Name}"/>

        <Label Content=" = " />

        <TextBlock Width="Auto" MinWidth="50" Padding="2" Text="{Binding Path=Vertices[0]}" />

        <Label Content=" " />

        <TextBlock Width="Auto" MinWidth="50" Padding="2" Text="{Binding Path=Vertices[1]}"/>

        <Label Content=" " />

        <TextBlock Width="Auto" MinWidth="50" Padding="2" Text="{Binding Path=Vertices[2]}"/>

        <Label Content=" " />

        <TextBlock Width="Auto" MinWidth="50" Padding="2" Text="{Binding Path=Vertices[3]}"/>

        <Button Content="Delete" Name="DeleteButton" Margin="5,0,0,0" />

    </StackPanel>


</UserControl>
